# Worker Threading Configuration
WORKER_ENABLED=true              # Set to 'true' to enable multi-threading, 'false' for legacy mode
BOTS_PER_WORKER=50               # Number of bots to assign to each worker thread
MAX_CONCURRENT_INIT=10           # Maximum number of bots to initialize concurrently
WORKER_TIMEOUT=60000             # Timeout for worker initialization in milliseconds

# Bot Configuration  
ACCOUNTS_FILE=accounts.txt       # Path to the accounts file
SESSION_PATH=./sessions          # Path to store bot sessions
PROXY_URL=                       # Optional proxy URL for bots
MAX_RETRIES=3                    # Maximum retries for bot operations
BOT_DEBUG=false                  # Enable detailed bot status logging

# Service Configuration
PORT=3000                        # Service port
DEBUG=false                      # Enable debug logging
QUEUE_TIMEOUT=5000               # Timeout for queue operations in milliseconds
MAX_QUEUE_SIZE=100               # Maximum queue size

POSTGRESQL_HOST=
POSTGRESQL_PORT=
POSTGRESQL_USER=
POSTGRESQL_PASSWORD=
POSTGRESQL_DB=
REDIS_HOST=
REDIS_PORT=
REDIS_PASSWORD=
POSTGRESQL_LOGGING=false
GC_DEBUG=true
ALLOW_REFRESH=false

#share some love with pricempire with this flag
PING_PRICEMPIRE=true

# Importing data from old csfloat database
POSTGRESQL_HOST_SOURCE=
POSTGRESQL_PORT_SOURCE=
POSTGRESQL_USER_SOURCE=
POSTGRESQL_PASSWORD_SOURCE=
POSTGRESQL_DB_SOURCE=
